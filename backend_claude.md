# CLAUDE.md — 쿨루프 모니터링 플랫폼 백엔드

> 오준택(백엔드) 개발 컨텍스트. 새 세션에서 먼저 읽을 것.
> 그린리모델링 챌린지 부문2 · AI 플랫폼 아이디어.

---

## 1. 이 백엔드가 하는 일

쿨루프 시공 건물의 옥상 표면온도(센서) + 외기 날씨(API)를 수집·저장하고,
**주기적으로 배치 분석**해서 쿨루프 성능이 유지되는지 판정하고 재도장 시점을 안내한다.
분석 결과는 **통계 로직 + AI 요약** 두 층으로 생성한다.

무게중심: **배치 분석**. (실시간 수신은 데이터 적재까지만, 무거운 분석은 배치)

---

## 2. 기술 스택

- Java 21 / Spring Boot 4.x
- Spring Web (REST), Spring Data JPA
- MySQL 9.6 (데이터 저장)
- Spring Batch 또는 @Scheduled (주기 분석)
- Redis (분석 결과 캐싱, 선택)
- 외부 연동: 기상청/OpenWeather API, 생성형 AI API(요약)
- 데이터 수신원: ESP32(MLX90614) → HTTP POST

---

## 3. 데이터 흐름

```
[ESP32] --HTTP POST(표면온도 JSON)--> [수신 API]
                                          ↓ 즉시
                              [기상청 API 호출: 외기온도·일사량]
                                          ↓ 병합(DTO)
                                     [MySQL 원시 적재]
                                          ↓ 주기 실행
                                  [배치 분석: 통계 판정]
                                          ↓
                                  [AI 요약: 행동방향 생성]
                                          ↓
                              [분석결과 저장 → 웹 대시보드 조회]
```

- 실시간 파트: 수신 + 날씨 병합 + 적재까지
- 배치 파트: 적재된 데이터를 주기(예: 1일 1회)로 분석·판정·요약

---

## 4. 도메인 모델 (MySQL 테이블)

### building (건물)
- id (PK)
- name
- address
- usage_type (용도)
- built_year (준공연도)
- coolroof_date (쿨루프 시공일) — 전후 구분 기준점
- is_verified (시공 인증 여부) — 거짓입력 방어용
- lat, lng (기상 API 조회용)

### temp_reading (표면온도 측정 원시데이터)
- id (PK)
- building_id (FK)
- surface_temp (센서 표면온도)
- outdoor_temp (API 외기온도)
- solar_radiation (API 일사량)
- weather (맑음/흐림 등)
- measured_at (타임스탬프)

### analysis_result (배치 분석 결과)
- id (PK)
- building_id (FK)
- period (분석 기간)
- avg_temp_gap (평균 표면-외기 온도차)
- peer_gap (동종 쿨루프 건물 평균 온도차)
- status (정상 / 주의 / 재도장권장)
- degradation_trend (저하 추세 지표)
- repaint_forecast (예상 재도장 시점)
- ai_summary (AI 생성 행동방향 텍스트)
- created_at

---

## 5. 핵심 분석 로직 (배치)

### 지표 1: 순수 쿨루프 효과 = 표면온도 - 외기온도
- temp_gap = surface_temp - outdoor_temp
- 작을수록 반사 잘 됨(정상), 클수록 열화

### 지표 2: 유효 데이터 필터링 (중요)
- 맑은 날 + 낮 시간대(예: 12~15시) 데이터만 분석에 사용
- 흐림·야간은 쿨루프 효과가 안 나타나므로 제외
- 일사량(solar_radiation) 기준으로도 필터 가능

### 지표 3: 동종 건물 비교 (peer comparison)
- 같은 용도·준공연도대·시공경과연차 유사한 쿨루프 건물군과 temp_gap 비교
- "동종 평균 대비 이 건물이 이상하게 높음" → 개별 이상 감지

### 지표 4: 저하 추세
- 시공 후 연차별 temp_gap 추이 (1년차→2년차→3년차)
- 우상향(온도차 벌어짐) = 반사성능 저하 진행

### 판정 규칙 (임계치 — 목업 기준, 조정 가능)
- temp_gap 정상범위 이내 + 추세 안정 → 정상
- temp_gap 상승 추세 → 주의
- temp_gap 임계 초과 or 동종 대비 이탈 → 재도장권장

### 재도장 시점 예측
- 저하 추세 선형/회귀 연장 → temp_gap이 임계치 도달하는 시점 산출

---

## 6. AI 요약 파트 (통계 + AI 병행)

- 통계 로직이 낸 수치·판정을 **AI API에 넘겨 자연어 행동방향으로 요약**
- 입력 예: 건물명, temp_gap 추세, 동종 비교, 판정 status
- 출력 예: "이 건물은 시공 3년차로 반사성능이 동종 대비 15% 저하. 다음 여름 전 재도장 검토 권장."
- 구현: 배치 분석 후 프롬프트 구성 → AI API 호출 → ai_summary 저장
- 원칙: 수치·판정은 통계가 확정, AI는 **해석·요약·문장화만** 담당 (환각 방지)

---

## 7. REST API (초안)

### 수신
- `POST /api/readings` — ESP32가 표면온도 전송 (수신 즉시 날씨 병합·적재)

### 조회 (대시보드용)
- `GET /api/buildings` — 건물 목록 + 최신 status
- `GET /api/buildings/{id}` — 건물 상세
- `GET /api/buildings/{id}/readings?from=&to=` — 온도 시계열
- `GET /api/buildings/{id}/analysis` — 최신 분석 결과(판정·AI요약)
- `GET /api/dashboard/summary` — 전체 건물 정상/주의/재도장 집계

### 배치 트리거 (내부/수동)
- `POST /api/batch/analyze` — 분석 배치 수동 실행 (시연용)

---

## 8. 배치 설계

- 스케줄: 1일 1회(@Scheduled) 또는 수동 트리거
- 순서: 유효데이터 필터 → 건물별 temp_gap 집계 → 동종 비교 → 추세·판정 → AI 요약 → analysis_result 저장
- 시연 팁: 목업 데이터 넣고 수동 트리거로 즉시 결과 생성

---

## 9. 목업 데이터 전략

- 실제 센서·시공 데이터 없음 → 목업으로 "축적된 상태" 구현
- 근거 수치 반영 (실증 기반):
  - 관악구: 쿨루프 vs 일반 22℃ 차, 냉방 20% 절감
  - 서울시 SH: 표면온도 9.2℃↓, 냉방 평균 26.4% 절감
- 목업에 반드시 포함할 시나리오:
  - 정상 건물 (temp_gap 안정)
  - 조기 열화 건물 (2년만에 급상승) → 경보 시연
  - 재도장 임박 건물 (임계 도달) → 예측 시연
- 구조: 건물 20~50개 × 시공 전후 수년치 월별/일별 데이터
- **설계 원칙: 목업 자리에 실제 API/센서만 꽂으면 되는 구조** → 실현가능성 증명

---

## 10. 오준택 담당 범위

- 데이터 수신 API + 날씨 API 병합 + 적재
- 배치 분석 로직 (temp_gap·필터·동종비교·추세·판정)
- 재도장 시점 예측
- AI 요약 연동
- 조회 API (대시보드 백엔드)
- 목업 데이터 생성기

강점 활용: Spring Boot, MySQL, Redis(캐싱), 스케줄링.
커리어 연결: 데이터 수집→분석→경보 = 시스템 모니터링/운영 경험.

---

## 11. 팀 인터페이스

- 임지혁(HW): ESP32 → `POST /api/readings` JSON 규격 협의 (`{"buildingId":.., "surfaceTemp":..}`)
- 김태윤(FE): 조회 API 응답 규격 협의 (대시보드 필드)
- 김찬우(보고서): 분석 로직·근거 수치 제공

---

## 12. 미확정 / 결정 필요

- [ ] temp_gap 정상/주의/재도장 임계치 구체값 (건축팀 도메인 근거 필요)
- [ ] 배치 주기 (1일 / 1주)
- [ ] AI API 종류 (GPT / Claude) 및 프롬프트
- [ ] 동종 건물 군집 기준 (용도·연차 묶는 방식)
- [ ] 냉방에너지 절감 산출 포함 여부 (건물에너지 API 연동)
