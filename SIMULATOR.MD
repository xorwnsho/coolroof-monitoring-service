# Claude Code 프롬프트 — Cool Roof Thermal Simulator 이식

아래 블록 전체를 Claude Code에 그대로 붙여넣으십시오.

---

## [붙여넣을 프롬프트 시작]

**Cool Roof Thermal Simulator** 인터랙티브 컴포넌트를 제작합니다. 아래 명세를 픽셀 단위로 준수하십시오. 임의 디자인 변경 금지입니다.

### 0. 기술 제약
- 단일 컴포넌트 파일로 구현합니다. 외부 애니메이션 라이브러리(framer-motion, gsap 등) 사용 금지입니다.
- 정적 장면(하늘/지면/건물/지붕/태양/온도 배지)은 **SVG**, 입자(광자·열기·반사광)는 SVG 내부에 함께 렌더링합니다.
- 애니메이션은 `requestAnimationFrame` 단일 루프로 처리합니다. 입자 배열은 `useRef`에 보관하고 프레임마다 상태를 한 번만 갱신합니다.
- 컴포넌트 언마운트 시 rAF를 반드시 해제합니다.
- `prefers-reduced-motion: reduce` 대응: 입자 생성을 중단하고 정적 장면 + 온도 값만 표시합니다.

### 1. 전체 레이아웃
카드 하나로 구성합니다. 위에서 아래로 4개 블록이 세로 배치됩니다.

1. **타이틀**: `Cool Roof Thermal Simulator`
2. **시뮬레이션 장면** (SVG, 가로 꽉 참)
3. **온도 표시부** (가운데 정렬)
4. **컨트롤 바** (가로 1행)

- 카드 배경: `#FAFAFA`
- 카드 좌우 패딩: `28px`, 상단 패딩: `24px`, 하단 패딩: `20px`
- 타이틀과 장면 사이 간격: `18px`
- 장면과 온도 표시부 사이 간격: `36px`
- 온도 표시부와 컨트롤 바 사이 간격: `40px`
- 카드 모서리 둥글기: `0`(부모 컨테이너에 맞춤). 카드 자체 그림자 없음.

### 2. 타이포그래피
- 폰트 스택: `'Google Sans', 'Google Sans Text', 'Product Sans', Roboto, 'Noto Sans KR', -apple-system, 'Helvetica Neue', Arial, sans-serif`
- 타이틀: `32px` / `font-weight: 400` / `color: #1F1F1F` / `letter-spacing: -0.2px`
- `SURFACE TEMP` 라벨: `11px` / `font-weight: 500` / `letter-spacing: 1.2px` / `text-transform: uppercase` / `color: #5F6368`
- 온도 값 (`81°C` 형태): `18px` / `font-weight: 700` / `color: #202124` / 라벨 아래 `6px` 간격
- 컨트롤 라벨(`Solar Intensity`, `Roof Type`): `16px` / `font-weight: 400` / `color: #202124`
- 배지 내부 온도 텍스트: `14px` / `font-weight: 500` / `color: #202124`

### 3. 시뮬레이션 장면 (SVG)
`viewBox="0 0 800 330"`, `width: 100%`, `preserveAspectRatio="xMidYMid slice"`, 모서리 둥글기 없음.

#### 3-1. 배경
- 하늘: 세로 선형 그라데이션 `#E9EDF5`(top) → `#E3E5EE`(bottom). 영역 `y = 0 ~ 282`
- 지면: `y = 282 ~ 330`, 단색 `#D6D8DC`. 상단 경계선 `#CDD0D5` 1px

#### 3-2. 건물
- 벽체: `x = 315 ~ 712`, `y = 230 ~ 282`. fill `#E8EAEF`(불투명도 0.6), stroke `#D4D7DE` 1px
- 처마 가로선: `(302, 230) → (726, 230)`, stroke `#D8DDE1` 1.5px
- 지붕(박공): 좌측 처마 `(302, 230)` → 용마루 `(512, 163)` → 우측 처마 `(726, 230)`
    - 지붕 본체: 위 경로를 stroke-width `10`, `stroke-linejoin: miter`로 그립니다. 채움 없음.
    - 지붕 아래쪽에 밝은 언더라인 1겹 추가: 동일 경로를 `y + 7`만큼 내려 stroke `#D8DDE1` 3px로 그립니다. (영상에서 지붕 아랫면이 밝은 회색으로 보이는 효과)

#### 3-3. 태양
- 중심 `(197, 81)`, 본체 원 반지름 `34`, fill `#F9A825`
- 글로우: 동일 중심 반지름 `62` 원, radial gradient `rgba(249,168,37,0.55)` → `rgba(249,168,37,0)`
- 태양은 회전·이동 없음. 크기 고정입니다.

#### 3-4. 온도 배지
- 용마루 위쪽에 말풍선 형태로 고정 배치합니다. 배지 중심 `(512, 112)`, 아래쪽 삼각 꼬리가 용마루를 향합니다.
- 배지: 흰색 fill `#FFFFFF`, 둥글기 `10px`, 좌우 패딩 `14px`, 높이 `34`, stroke `2px`
- stroke·꼬리 색상은 **지붕 타입**에 따라 결정합니다 (온도 기준 아님).
    - Dark: `#C5221F`
    - White: `#1A73E8`
- 텍스트는 정수 + `°C` (예: `54°C`)

### 4. 지붕 타입별 물리·시각 규칙

| 항목 | Dark | White |
|---|---|---|
| 흡수율(absorptivity) | `0.65` | `0.15` |
| 지붕 색 | 온도 보간 | 고정 `#FFFFFF` |
| 광자 충돌 시 효과 | 열기 뭉치(heat blob) 생성 | 반사광(ray) 생성 |
| 배지 색 | `#C5221F` | `#1A73E8` |

#### 4-1. 온도 계산식
```
temp = Math.round(20 + absorptivity * solarIntensity)
```
- `solarIntensity` 범위: `0 ~ 100` (정수)
- 검증 케이스(반드시 일치해야 합니다): Dark 0→20, Dark 9→26, Dark 53→54, Dark 94→81, White 5→21, White 11→22, White 92→34
- 표시 온도는 목표값으로 **0.12 계수 lerp** 하여 부드럽게 수렴시킵니다. 배지와 `SURFACE TEMP` 값은 동일한 보간 값을 사용합니다.

#### 4-2. 지붕 색 보간 (Dark)
- 20°C: `#1F2A37` (짙은 남색)
- 85°C: `#8E1B1B` (짙은 적색)
- 그 사이는 RGB 선형 보간합니다. 온도 변화 시 색이 실시간으로 따라 변해야 합니다.

#### 4-3. 지붕 색 (White)
- fill/stroke `#FFFFFF`, 외곽선 `#DDE1E6` 1px
- 용마루 쪽이 살짝 더 밝게 보이도록 선형 그라데이션 `#FFFFFF` → `#F0F2F5` 적용합니다.
- 온도가 올라가도 White 지붕 색은 변하지 않습니다.

### 5. 입자 시스템

#### 5-1. 광자 (Photon) — 공통
- 생성: 매 프레임 `Math.random() < solarIntensity / 100 * 0.35` 조건에서 1개 생성합니다. `solarIntensity === 0`이면 생성하지 않습니다.
- 동시 존재 상한: `40`개
- 시작 위치: 태양 중심 `(197, 81)` 기준 반지름 `0~30` 랜덤 오프셋
- 목표 지점: 좌측 지붕면 `(302,230) ~ (512,163)` 선분 위 랜덤 한 점
- 이동: 시작→목표 정규화 벡터 × 속도 `1.8 ~ 2.6` (랜덤)
- 렌더: 원, 반지름 `4 ~ 6` 랜덤, fill `#F9A825`, 불투명도 `1`
- 목표 도달 시 즉시 제거하고 충돌 효과를 1개 생성합니다.

#### 5-2. 열기 뭉치 (Dark 전용)
- 충돌 지점에서 생성합니다.
- 이동: `vx = -0.35 ~ -0.9`, `vy = -0.6 ~ -1.1` (좌상단으로 상승)
- 반지름: 초기 `18` → 매 프레임 `+0.18` 증가, 최대 `48`
- 색: `rgba(176, 58, 58, 0.16)` 단색 원 (테두리 없음)
- 수명 `140`프레임, 수명의 마지막 40%에서 불투명도를 선형 페이드아웃합니다.
- 태양 근처까지 겹겹이 쌓이며 올라가는 모습이 되어야 합니다.

#### 5-3. 반사광 (White 전용)
- 충돌 지점에서 생성합니다.
- 짧은 직선 세그먼트입니다. 길이 `26 ~ 36`, 각도는 좌상단 방향 `-135° ± 15°` 랜덤
- stroke-width `3`, `stroke-linecap: butt`
- 색: `#4A4F57`과 `#C9CED6` 중 랜덤 선택 (두 톤이 섞여 보여야 합니다)
- 이동: 자신의 각도 방향으로 속도 `1.2 ~ 1.8`
- 수명 `110`프레임, 마지막 50%에서 페이드아웃합니다.

#### 5-4. 타입 전환
- `Roof Type` 변경 시 기존 입자는 제거하지 않고 각자 수명까지 유지한 뒤 소멸시킵니다. 신규 생성분만 새 타입 효과를 따릅니다.

### 6. 컨트롤 바
가로 1행 flex, `align-items: center`, 요소 간 간격 `20px`.

순서: `Solar Intensity` 라벨 → 슬라이더 → 숫자 박스 → `Roof Type` 라벨 → 셀렉트

#### 6-1. 슬라이더
- `<input type="range" min="0" max="100" step="1">`
- 트랙 높이 `4px`, 둥글기 `2px`
- 채워진 구간 `#1F1F1F`, 빈 구간 `#DADCE0`
- 썸: 세로 알약 형태. 너비 `12px`, 높이 `30px`, 둥글기 `6px`, fill `#1F1F1F`
- 썸 주위에 `3px` 흰색 여백 링을 주어 트랙과 분리되어 보이게 합니다.
- 너비: `220px` 고정
- 드래그 중 즉시 반영(`onChange`, `onInput` 모두 처리)

#### 6-2. 숫자 박스
- 읽기 전용 표시입니다. 배경 `#F1F3F4`, 둥글기 `12px`, 패딩 `14px 34px`, 텍스트 `16px` 중앙 정렬, 색 `#202124`
- 최소 너비 `96px`

#### 6-3. 셀렉트 (Roof Type)
- 옵션: `Dark`, `White` (기본값 `Dark`)
- 완전 알약 형태: 둥글기 `9999px`, 배경 `#F1F3F4`, 테두리 없음(포커스 시 `1px solid #1F1F1F`)
- 패딩 `13px 22px`, 텍스트 `16px`, 색 `#202124`
- 우측에 작은 삼각형 드롭다운 화살표(`#5F6368`)를 `18px` 안쪽에 배치합니다.
- 남은 가로 공간을 채우도록 `flex: 1` 처리합니다.

### 7. 초기 상태
- `solarIntensity = 50`
- `roofType = 'Dark'`
- 마운트 즉시 애니메이션이 동작해야 합니다.

### 8. 반응형
- 컨테이너 너비 `720px` 미만: 컨트롤 바를 2행으로 줄바꿈합니다. (1행: 라벨+슬라이더+숫자, 2행: 라벨+셀렉트)
- 슬라이더 너비를 `flex: 1`로 전환합니다.
- SVG 장면은 항상 가로 100%, 비율 유지합니다.
- 타이틀은 `640px` 미만에서 `26px`로 축소합니다.

### 9. 접근성
- 슬라이더에 `aria-label="Solar Intensity"` 및 `aria-valuetext="{value}"` 지정
- 셀렉트에 `aria-label="Roof Type"` 지정
- 온도 표시부에 `aria-live="polite"` 지정
- SVG는 `role="img"`, `aria-label`로 현재 상태를 서술합니다.

### 10. 검수 체크리스트
작업 완료 후 아래 항목을 직접 확인하고 보고하십시오.

- [ ] Dark / 94 → `81°C` 표시
- [ ] White / 92 → `34°C` 표시
- [ ] Solar Intensity `0`에서 광자·열기·반사광이 전혀 생성되지 않음
- [ ] Solar Intensity를 올릴수록 광자 개수가 눈에 띄게 증가
- [ ] Dark에서 붉은 열기 뭉치가 지붕 → 좌상단 태양 방향으로 상승
- [ ] White에서 짧은 반사광 선분이 지붕 → 좌상단으로 튕겨 나감
- [ ] Dark 지붕 색이 온도 상승에 따라 남색 → 적색으로 변화
- [ ] 배지 테두리 색이 Dark=빨강 / White=파랑으로 전환
- [ ] 60fps 유지, 콘솔 에러 없음

## [붙여넣을 프롬프트 끝]

---

## 참고 — 원본 영상에서 추출한 실측값

| 측정 항목 | 값 |
|---|---|
| Dark, intensity 0 | 20°C |
| Dark, intensity 9 | 26°C |
| Dark, intensity 53 | 54°C |
| Dark, intensity 94 | 81°C |
| White, intensity 5 | 21°C |
| White, intensity 11 | 22°C |
| White, intensity 92 | 34°C |

→ 역산 결과 `temp = 20 + k × intensity`, Dark `k=0.65`, White `k=0.15`

---

## [추가 작성이 필요한 부분]

프롬프트를 넣기 전에 아래 3가지를 프롬프트 맨 앞에 덧붙이십시오.

1. **프레임워크·파일 경로** — 예: `Next.js App Router. src/components/CoolRoofSimulator.tsx 로 생성. Tailwind 사용.`
2. **폰트 처리 방침** — `Google Sans`는 비공개 폰트입니다. 대체안 중 택일이 필요합니다.
    - (a) Google Fonts의 `Roboto` 사용 — 가장 근접
    - (b) 프로젝트 기존 폰트 유지
3. **삽입 위치** — 기존 페이지 어느 섹션 아래에 넣을지 명시